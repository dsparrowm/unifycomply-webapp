# Upstream UnifyComply Core Platform (server-only — never expose to the browser)
API_BASE_URL=https://unifycomply.svr.monolith.ng

# Public app origin used for Google OAuth redirectUrl (optional in local; defaults to request origin)
# APP_URL=http://localhost:3000
