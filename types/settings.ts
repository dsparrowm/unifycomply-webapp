export type SettingsProfile = {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  role: string;
  department: string;
  timezone: string;
  language: string;
  initials: string;
  displayName: string;
};

export type SettingsSelectOption = {
  label: string;
  value: string;
};
